import 'package:flutter/material.dart';

class Radii {
  static const BorderRadiusGeometry k12pxRadius = BorderRadius.all(
    Radius.circular(12),
  );

  static const BorderRadiusGeometry k20pxRadius = BorderRadius.all(
    Radius.circular(20),
  );

  static const BorderRadiusGeometry k6pxRadius = BorderRadius.all(
    Radius.circular(6),
  );
}
